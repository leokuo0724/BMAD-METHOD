# Knowledge Builder

智慧學習工具，專為軟體工程師設計，快速消化網路技術文章。

## 概述

Knowledge Builder 透過 AI 驅動的內容分析，將任何網址的技術文章轉換為結構化、可搜尋、多語言的知識筆記。

**主要功能**:

- 🌐 **智慧內容解析** - 使用 Playwright 瀏覽器自動化處理動態網頁
- 🧠 **脈絡化內容萃取** - 強調 WHY (為何) 和 HOW (如何) 的理解
- 💡 **問題導向** - 提取文章解決的問題背景與動機
- 🎯 **心智模型** - 捕捉概念框架、類比和思考方式
- 💻 **內嵌程式碼範例** - 程式碼與說明脈絡整合,不分離
- 🌍 **多語言翻譯** - 自動翻譯為偏好語言
- 🏷️ **智慧標籤** - 自動生成主題標籤
- 📊 **知識圖譜** - 建立概念間的連結 (Phase 2)
- 📤 **多格式匯出** - 支援不同輸出格式 (Phase 3)

## 安裝

### 基本安裝

```bash
bmad install knowledge-builder
```

### Playwright MCP 設定 (推薦 - 用於動態網頁抓取)

Knowledge Builder 使用 Microsoft Playwright MCP Server 來抓取動態網頁內容。

**安裝 Playwright MCP**:

1. 確保您的 Claude Code 已配置 MCP 支援
2. 在您的 MCP 配置中加入 Playwright server (推薦使用 headless mode):

```json
{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@microsoft/mcp-server-playwright", "--headless"]
    }
  }
}
```

**Headless Mode 說明**:

- `--headless` flag: 瀏覽器在背景執行,不會彈出視窗
- 推薦日常使用,避免干擾工作流程
- 若需要除錯,可移除 `--headless` 查看瀏覽器實際操作

3. 重啟 Claude Code 以載入 MCP server

**替代方案**: 如果不使用 Playwright MCP,模組會退回使用簡單的 HTTP fetch,但可能無法處理需要 JavaScript 渲染的網頁。

詳細配置選項請參考 [INSTALLATION.md](INSTALLATION.md)。

## 組件

### Agents (2)

#### 1. Knowledge Curator (主要代理)

負責內容擷取與知識管理

**主要指令**:

- `/digest [url]` - 消化單一網址並生成知識筆記
- `/translate [language]` - 翻譯內容為指定語言
- `/summarize` - 生成執行摘要

#### 2. Tag Master (專業代理)

負責智慧標記與知識組織

**主要指令**:

- `/tag [content]` - 為內容自動生成標籤
- `/categorize` - 將內容分類到知識體系
- `/relate [topic]` - 找出相關知識連結

### Workflows (8)

**核心 Workflows**:

1. `digest-article` - 將單一網址轉換為結構化知識筆記
2. `batch-digest` - 批次處理多個網址
3. `search-knowledge` - 搜尋和檢索知識庫

**功能 Workflows**: 4. `update-knowledge` - 更新已存在的知識筆記 5. `export-knowledge` - 匯出知識庫為不同格式 6. `build-knowledge-graph` - 視覺化知識關聯

**工具 Workflows**: 7. `manage-templates` - 管理和自訂知識筆記模板 8. `configure-preferences` - 設定個人偏好

## 快速開始

1. **載入主要 agent:**

   ```
   /bmad:kb:agent:curator
   ```

2. **配置偏好:**

   ```
   /configure
   ```

3. **消化第一篇文章:**
   ```
   /digest https://example.com/article
   ```

## 模組結構

```
knowledge-builder/
├── agents/                    # Agent 定義
│   ├── curator.agent.yaml
│   └── tag-master.agent.yaml
├── workflows/                 # Workflow 資料夾
│   ├── digest-article/
│   ├── batch-digest/
│   ├── search-knowledge/
│   └── ...
├── templates/                 # 共用模板
│   └── technical-article.md
├── data/                      # 模組資料
└── config.yaml               # 模組配置
```

## 配置

模組可在 `.bmad/knowledge-builder/config.yaml` 中配置

**關鍵設定**:

- `translation_language` - 翻譯目標語言 (繁中/簡中/英文/日文)
- `default_template` - 預設知識模板
- `knowledge_base_path` - 知識庫儲存路徑
- `auto_tagging` - 啟用自動標記
- `use_browser_automation` - 是否使用瀏覽器自動化 (預設: true, 需要 Playwright MCP)
- `detail_level` - 筆記詳細程度 (minimal/standard/detailed)

## 範例

### 範例 1: 快速學習新技術

```
/digest https://reactjs.org/docs/hooks-intro.html
```

系統自動：

1. **內容抓取**: 使用 Playwright 擷取完整渲染的文章內容
2. **結構分析**: 生成文章大綱,識別主要章節
3. **脈絡萃取**:
   - 提取問題背景 (WHY: 為何這個主題重要?)
   - 核心概念與原理 (WHAT + WHY: 是什麼以及為何如此設計)
   - 實際影響與應用 (HOW: 如何使用以及何時使用)
   - 心智模型 (如何思考這個主題)
   - 程式碼範例內嵌在技術說明中
4. **翻譯**: 翻譯為繁體中文,保留程式碼和技術術語
5. **標記**: 自動標記 #react #hooks #frontend #architecture
6. **儲存**: Markdown 格式

**產出範例**:

```
knowledge-base/
└── react-hooks-intro.md        (包含完整脈絡化筆記與內嵌程式碼)
```

### 範例 2: 建立主題知識庫

```
/batch-digest graphql-articles.txt
```

批次處理多篇 GraphQL 文章，自動建立主題知識庫。

## 開發路線圖

- [x] Phase 1: MVP - 基本消化功能
- [ ] Phase 2: 智慧標記與搜尋
- [ ] Phase 3: 知識圖譜與進階功能

## 貢獻

使用 `create-agent` 和 `create-workflow` 工作流程擴展此模組。

## 作者

Created by BMad on 2025-11-18

---

**模組主題**: 知識花園 (Knowledge Garden)
**版本**: 1.0.0
**狀態**: Ready for Development
